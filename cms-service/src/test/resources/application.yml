spring:
  ldap:
    # Embedded Spring LDAP for tests
    embedded:
      base-dn: dc=memorynotfound,dc=com
      credential:
        username: uid=admin
        password: secret
      ldif: classpath:schema.ldif
      port: 12345
      validation:
        enabled: false

cms:
  storages:
    image:
      type: fs
      #uri: /
      root: /home/vd/ASSET/WS/src/java/Exadel/EOM/cms-service/target/test-classes/storages/images/
    image-zip:
      type: fs
      uri: jar:file:/home/vd/ASSET/WS/src/java/Exadel/EOM/cms-service/target/test-classes/storages/images1.zip
      root: /images/
    ldap:
      type: ldap
      url: ldaps://ldap.vn.exadel.com:636
      userDn: cn=LDAPaccess,ou=Special,dc=exadel,dc=com
      password: ${LDAP_PASSWORD}
      userSearchBase: ou=addressbook,dc=exadel,dc=com
      userSearchFilter: (uid={0})